name: GitHub Metrics

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

jobs:
  github-metrics:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - name: Generate GitHub Metrics
        uses: lowlighter/metrics@v3.34
        with:
          token: ${{ secrets.METRICS_TOKEN }}

          user: gobzzz-g
          filename: github-metrics.svg

          # Full-width two-column layout
          config_display: columns

          # Keep repositories out of the dashboard
          config_order: base.header, base.activity+community, languages, isocalendar, topics

          # Base sections
          base: header, activity, community

          # Contribution calendar
          plugin_isocalendar: yes
          plugin_isocalendar_duration: half-year

          # Programming languages
          plugin_languages: yes
          plugin_languages_sections: most-used
          plugin_languages_limit: 8

          # Starred topics
          plugin_topics: yes
          plugin_topics_limit: 10

          # Prevent the isometric calendar graphic from being pushed
          # behind the previous section in the columns layout.
          extras_css: |
            svg.columns section > svg {
              margin-top: 0 !important;
            }
